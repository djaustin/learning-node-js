// Storing the strings to be used for event emitter here so we can reference the property and only have the string typed here
module.exports = {
  events:{
    GREET: 'greet',
    FILESAVED: 'filesaved',
    FILEOPENED: 'fileopened'
  }
}
